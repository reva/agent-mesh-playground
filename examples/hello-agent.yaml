apiVersion: kagent.dev/v1alpha2
kind: Agent
metadata:
  name: hello
  namespace: kagent
spec:
  type: Declarative
  description: Smoke-test agent. Confirms the model path to OpenRouter works.
  declarative:
    stream: true
    modelConfig: default-model-config
    systemMessage: You are a terse assistant. Answer in one short sentence.
